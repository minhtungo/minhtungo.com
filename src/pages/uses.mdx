import { MetaData, Layout } from '@/components/Layout';
import Head from 'next/head';
import { OFFICE_ITEMS, CODING, OTHER_TECH } from '@/config/uses';
import pageMeta from '@/config/meta';

## Uses

Here's my current technology setup for coding, gaming, and daily life.

### Computer / Office

<ItemGrid items={OFFICE_ITEMS} />

### Coding

<ItemGrid items={CODING} />

### Other Tech

<ItemGrid items={OTHER_TECH} />

export default ({ children }) => (
  <>
    <MetaData
      title={pageMeta.uses.title}
      description={pageMeta.uses.description}
    />
    <Layout>{children}</Layout>
  </>
);
